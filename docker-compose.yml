version: '2'

services:

  solr:
    image: solr:5.4.1
    restart: always
    volumes:
      - ./solr:/opt/solr/server/solr
    ports:
      - 8983:8983

  compile:
    build:
      context: .
      dockerfile: Dockerfile-compile
    volumes:
      - .:/project
    
    